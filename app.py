from window import Window

if __name__ == "__main__":
    window = Window()

# Predecir:
# X: solo numeros o strings(mapeadas < 10)
# Y: solo numeros, no strings, quitar la columna de las seleccionadas en X

# Clasificar:
# X: todo
# Y: solo strings, mapeadas < 10    